import { motion } from "framer-motion";
import { pageTransition } from "../../../shared/motion/animations";
export default function InterviewsList() {
  return (
    <motion.div {...pageTransition}>Interviews</motion.div>
  );
}
